package dp.leetcode.regularExp;

public class Solution {
	
	
    /*public boolean isMatch(String s, String p) {
    	boolean isMatch = true;
    	
    	String sPart;
    	String spc;
    	String pPart;
    	String prevChar;
    	int pIdx;
    	int sIdx;
    	while(){
    		//一直读，直到读到一个特殊字符
    		pPart = ;
    		spc = ;
    		
    		//读的只是一个.
    		
    		//读的只是一个*
    		if( ".".equals(prevChar) ){
    			if( p后面无普通字符 ){//**是error??
    				return true;
    			}else{//p后面有普通字符
    				//在s中找p后面第一个普通字符 firstComCharIdx
    				if( 没有){
    					return false;
    				}else{//有
    					move(p,firstComCharIdx)
    					move(s,firstComCharIdx)
    				}
    			}
    		}else{
    			
    		}
    		 
    		
    		//如果特殊字符是.
    		
    		//待匹配字符段：
    		//读相同长度的sPart
    		sPart.substring(0,sPart.length()-1) pPart.substring(0,sPart.length()-1) 
    		
    		//如果特殊字符是*
    		//
    		
    	}
    }
    
    private void move(idx){
    	idx
    	part
    }*/
    
}


/*
pStr
compiled pattern


interface SpecialChar
class Dot implements SpecialChar{ 
class Star*/
